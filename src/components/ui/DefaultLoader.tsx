import { CircularProgress } from "@mui/material"

import '@styles/ui/DefaultLoader.css'

const DefaultLoader = () => {
  return <CircularProgress className="hack2024-default-loader"/>
}

export default DefaultLoader